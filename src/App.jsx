import { BrowserRouter, Router, Routes } from "react-router-dom";

const App = () => {
  return (
    <BrowserRouter>
      <Routes>
        <Router />
      </Routes>
    </BrowserRouter>
  );
};

export default App;
